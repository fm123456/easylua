PROJECT(EasyLua)
INCLUDE_DIRECTORIES(${EasyLua_SOURCE_DIR}/lib)
SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -g -std=c++11")

AUX_SOURCE_DIRECTORY(. DIR_SRCS)

ADD_LIBRARY(easylua STATIC ${DIR_SRCS})